你现在接手 Replicated Suite（ArcheRage RU Lua 客户端综合辅助插件），本会话没有旧聊天上下文。请进入连续执行的目标模式，推进当前工程中标记为“未完成”的既有工作，不要只产出计划，不要做完一个小改就等待我回复“继续”。我会在后面集中进行游戏实测。

【先读取交接包】
在可访问工作区定位 `ReplicatedSuite_Agent_Handoff/00_START_HERE.md`，依次读取 `01_CONTEXT_AND_RULES.md`、`02_UNFINISHED_WORKLIST.md`、`05_EVIDENCE_AND_VERIFICATION.md`，再读取包内 `skills/skillforge-archerage-addon/SKILL.md`（2026.09.13-r2）及当前任务涉及的参考。读取 `04_RUN_STATE.md` 判断是否需要续接。如果这些文件不在工作区，只询问交接包位置，不猜内容。

【本地源码是开工依据】
确认真正的AddOn根目录、`replicatedsuite.lua`、`toc.g`、V3运行链、本地API参考、当前技能和现有工程指令；保存包含用户已有未提交改动的只读起始基线。检查Git状态和并行修改。交接包是历史参考，不保证已在本机覆盖；BuildTag相同不能证明文件相同。差异先解释，不自动回退、重放历史补丁或覆盖用户更改。没有本地读写能力就明确报告，不假装已改文件。

【目标与队列】
从当前 `features/rs_feature_registry.lua`、导航判定、页面/命令、Authority/Service、测试和TODO重新建立任务表。历史种子有24项（19项侧栏可见、5项隐藏子项），数量和优先级必须以本地重新核对为准。区分真正未实现、已有实现待RU验收、只缺API/数据证据、仅文档状态滞后；不要把它们全部从零重写。
先处理会阻断本轮工作的已证实P0问题，再优先推进活动、任务追踪、债券/居民板的现有交互与保存闭环；随后选择有可靠接口的住宅/管家、制作、拍卖等任务。只检查确有证据的缺口，不把建议顺序当作每项都有bug。此前已交付的PVP HUD、首领倒计时、存档/复制、增益数量、随机商店和新默认布局主要进入回归/待验收队列，不无证据反复改。

【执行授权与边界】
已授权你对明确范围内的非破坏性代码、测试和相关文档连续实施，不必再次询问是否开始。每轮完成“读真实链路→写反例并确认失败原因→最小实现→相关回归→检查diff→记录结果”的完整闭环，然后直接开始下一项。不要扩张为全架构重写，不新增无关功能，不只改“未完成”标签凑进度。
保留V3-only、单入口、Services/EventBus/独立Feature生命周期和统一RSUI。这里不是UE工程，不引入UObject、OnRep或FastArray。关闭功能释放自己的任务/缓存/订阅但保留设置；隐藏HUD与关闭功能分开；死亡回顾等不能依赖DPS启动。
不得清空存档、跳过完整性校验、自动恢复所有默认值、绕过禁用API、探测未知槽位范围、强推仓库或覆盖其他Agent修改。真实游戏写操作、破坏性操作或不可推断的产品语义须保留门禁并单独确认。需要权限或实机证据的子任务记为阻塞，继续其他独立任务。

【正确性重点】
逐个修改点写维护注释：原因、Authority/数据流、兼容边界、实现理由和风险。保留连续事件合并的最早执行时间，分开位置刷新与事实更新；Native拒写不能记成功，旧target/generation不能污染新对象。未知不是0，敌人装备类型不是具体物品，当前挂单不是历史成交，预计收益不是实际到账。
冻结列表是持续观察留存，不是单帧快照。新UI组件要贯通校准、实际显示、导出、默认恢复和存档。保留“获取当前位置”与HUD V2专用编辑框导出；默认布局只作用于新建/明确恢复，不能改坏旧canonical。报告采用同ID固定正文、显式分页与回读容量校验；等待复制不能刷新选区或抢聊天焦点；保存失败仍保留插件重载修复出口。

【未知问题查证】
先读当前调用方、封装返回和清理链，再查 `z_api_functions` 的Allowed分类与 `core/rs_api_capabilities.lua`。新能力或权限变化查有日期的RU一手资料；社区代码只作语义线索，不搬旧底层或据此解禁。仅对获准接口做有界低频只读取证，记录返回个数/类型、单位、身份、上下文和时序。没有足够证据时记录查询内容、缺口、降级和最小补证需求，不能编造API/字段/技能ID/测试结论。

【验证与交付】
核对测试入口和依赖是否真实存在。Lua5.1、其他解释器+shim、Native模型和RU实机结果分别记录；跳过/缺失/历史通过不计入本轮成功，不能删失败用例求绿。只有离线通过的功能保持“已实现待实机”，不自动宣告产品完成。
每个闭环更新工作板和 `04_RUN_STATE.md`。最终按本次会话起始基线生成只含实际修改/新增文件的增量ZIP，保留相对路径；删除项单列，API参考只有改动才入包。用实际最终ZIP在确定基线上重建、复测并核对未修改文件，排除用户数据/缓存/交接包副本。
持续推进到安全可实施队列处理完，或只剩明确外部阻塞，或宿主会话预算将尽。不要空转、不要承诺后台无限执行；结束前保存可恢复检查点并交付已验证部分。最终集中输出：1完成内容，2修改文件，3性能影响，4兼容风险，5集中测试建议，以及阻塞项的最小补证需求。

现在开始读取交接材料和本地工程、核对基线并执行第一项，不要只回复“准备好了”。
