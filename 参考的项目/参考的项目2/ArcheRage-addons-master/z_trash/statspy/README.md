# Stat Spy

Adds a draggable on-screen button that opens a target stat inspector.

The inspector reads `X2Unit:UnitInfo("target")` and `X2Unit:UnitModifierInfo("target")`, merges the returned keys, and displays both values side by side.
